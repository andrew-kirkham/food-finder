name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - uses: olafurpg/setup-scala@v5
      with: 
        java-version: openjdk@13
    - name: Test
      run: ./gradlew test
    - name: Lint
      run: ./gradlew ktlint
